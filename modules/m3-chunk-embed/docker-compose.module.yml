services:
  m3-chunk-embed:
    build:
      context: ../..
      dockerfile: modules/m3-chunk-embed/Dockerfile
    ports:
      - "8103:8000"
    env_file: .env.example
    environment:
      MODULE_IMPL: ${MODULE_IMPL:-real}
    networks:
      - ragnet

networks:
  ragnet:
    external: true
