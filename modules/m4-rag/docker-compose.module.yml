services:
  m4-rag:
    build:
      context: ../..
      dockerfile: modules/m4-rag/Dockerfile
    ports:
      - "8104:8000"
    env_file: .env.example
    environment:
      MODULE_IMPL: ${MODULE_IMPL:-real}
    networks:
      - ragnet

networks:
  ragnet:
    external: true
