services:
  m5-gateway:
    build:
      context: ../..
      dockerfile: modules/m5-gateway/Dockerfile
    ports:
      - "8080:8000"
    env_file: .env.example
    environment:
      MODULE_IMPL: ${MODULE_IMPL:-real}
    networks:
      - ragnet

networks:
  ragnet:
    external: true
