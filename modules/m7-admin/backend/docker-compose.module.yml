services:
  m7-admin:
    build:
      context: ../../..
      dockerfile: modules/m7-admin/backend/Dockerfile
    ports:
      - "8107:8000"
    env_file: .env.example
    environment:
      MODULE_IMPL: ${MODULE_IMPL:-real}
    networks:
      - ragnet

networks:
  ragnet:
    external: true
