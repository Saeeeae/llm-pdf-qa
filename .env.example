# === Document Watch ===
DOC_WATCH_DIR=/data/documents

# === PostgreSQL ===
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=rag_system
POSTGRES_USER=admin
POSTGRES_PASSWORD=changeme

# === Qdrant ===
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_COLLECTION=documents

# === Embedding ===
EMBED_MODEL=intfloat/multilingual-e5-large
EMBED_DEVICE=cpu
EMBED_BATCH_SIZE=32

# === MinerU v2.x ===
# pipeline: CPU only (~82+ accuracy)
# hybrid-auto-engine: GPU 10GB+ VRAM (~90+ accuracy)
# vlm-auto-engine: GPU 8GB+ VRAM (~90+ accuracy)
MINERU_BACKEND=pipeline
MINERU_LANG=korean

# === FastAPI ===
API_HOST=0.0.0.0
API_PORT=8000

# === Celery ===
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# === Sync Schedule ===
SYNC_CRON_HOUR=2
SYNC_CRON_MINUTE=0

# === HuggingFace ===
HF_TOKEN=
CUDA_VISIBLE_DEVICES=0
